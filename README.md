# 子竹书法博客

基于 Next.js 14 (App Router) + TypeScript 构建的现代化博客系统，使用 Markdown 文件作为数据源。

## ✨ 特性

- 🚀 **Next.js 14** - 使用最新的 App Router
- 📝 **Markdown 支持** - 使用 Markdown 文件作为内容源
- 🎨 **保持原有设计** - 完全保留原有 HTML/CSS 样式
- 🔍 **SEO 友好** - 自动生成元数据和静态页面
- ⚡ **SSG 静态生成** - 构建时预生成所有页面
- 📱 **响应式设计** - 完美适配移动端和桌面端
- 🎯 **TypeScript** - 完整的类型安全

## 📁 项目结构

```
ablog/
├── app/                    # Next.js App Router
│   ├── about/             # 关于页面
│   ├── blog/              # 博客页面
│   │   ├── [slug]/        # 文章详情页（动态路由）
│   │   └── page.tsx       # 文章列表页
│   ├── layout.tsx         # 根布局
│   ├── page.tsx           # 首页
│   └── globals.css        # 全局样式
├── components/            # React 组件
│   ├── Header.tsx         # 头部导航
│   ├── Footer.tsx         # 页脚
│   ├── Sidebar.tsx        # 侧边栏（首页使用）
│   └── Layout.tsx         # 布局组件
├── content/              # 内容目录
│   └── posts/            # Markdown 文章
├── lib/                  # 工具函数与数据读取
│   ├── posts.ts          # 文章读取和解析
│   ├── constants.ts      # 常量配置
│   └── gallery.ts        # 作品集图片索引
├── types/                # TypeScript 类型定义
│   └── index.ts
├── public/               # 静态资源
│   ├── static/           # 样式和图片
│   └── gallery/          # 画廊图片
└── static/               # 源文件（样式和图片）
```

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
npm run build
```

### 启动生产服务器

```bash
npm start
```

## 📝 添加新文章

1. 在 `content/posts/` 目录下创建新的 `.md` 文件
2. 文件命名建议使用文章标题（将作为 URL slug）
3. 文件必须包含 frontmatter：

```markdown
---
title: 文章标题
date: 2024-01-15
tags: ["标签1", "标签2"]
summary: 文章摘要
---

文章正文内容...
```

## 🛠️ 技术栈

- **Next.js 14** - React 框架（App Router）
- **TypeScript** - 类型安全
- **gray-matter** - Frontmatter 解析
- **marked** - Markdown 转 HTML
- **date-fns** - 日期格式化

## 📚 核心模块

### lib/posts.ts

提供文章相关的工具函数：

- `getAllPostSlugs()` - 获取所有文章的 slug
- `getPostBySlug(slug)` - 根据 slug 获取单篇文章
- `getAllPosts()` - 获取所有文章（按日期降序）
- `getAllTags()` - 获取所有标签

### lib/constants.ts

应用常量配置：

- `SITE_CONFIG` - 网站基本信息
- `ROUTES` - 路由常量

### lib/gallery.ts

作品集相关功能：

- `getAllGalleryImages()` - 枚举 `public/gallery` 目录下的图片并返回可直接渲染的信息（src、alt、filename）

## 🎨 页面说明

- **首页** (`/`) - 使用侧边栏导航，显示书签样式内容
- **文章列表** (`/blog`) - 显示所有文章列表
- **文章详情** (`/blog/[slug]`) - 显示单篇文章内容
- **关于页面** (`/about`) - 网站介绍和联系方式

## 📝 注意事项

1. Markdown 文件必须包含完整的 frontmatter
2. 日期格式：`YYYY-MM-DD`
3. tags 必须是数组格式
4. 文章文件名将作为 URL slug，建议使用中文或英文
5. 静态资源放在 `public/` 目录下

## 📄 许可证

MIT
